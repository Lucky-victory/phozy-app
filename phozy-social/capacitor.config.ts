import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phozy.social',
  appName: 'phozy-social',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
